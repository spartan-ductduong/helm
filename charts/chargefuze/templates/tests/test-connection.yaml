{{- if .Values.testConnection.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chargefuze.fullname" . }}-test-connection"
  labels:
    {{- include "chargefuze.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chargefuze.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}
